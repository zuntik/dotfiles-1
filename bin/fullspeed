#!/bin/bash
if [[ $EUID -ne 0 ]]; then
	echo "run as root" 1>&2
	exit 1
fi

while :; do echo level disengaged > /proc/acpi/ibm/fan; sleep 3; done &
echo $! > /tmp/fullspeed.pid
#tlp ac
#tpacpi-bat -s IC 1 1
#xset -dpms

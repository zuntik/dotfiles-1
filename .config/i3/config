# vim:filetype=i3
# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod1

font pango:Monospace 10

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal
bindsym $mod+Ctrl+Return exec xterm

# kill focused window
bindsym $mod+q kill

# mouse bindings
# Left Mouse click for fullscreen --> Conflicts
#bindsym --release button1 fullscreen
#bindsym --border button1 fullscreen

# The middle button over a titlebar kills the window
#bindsym --release button2 kill
bindsym --border button2 kill
bindsym --whole-window $mod+button2 kill

# The right button toggles floating
#bindsym --release button3 floating toggle
bindsym --border button3 fullscreen

# 'bashrun' launcher -> just urxvt with a special rcfile
bindsym $mod+d exec urxvtc -title bashrun -geometry 70x4 -e sh -c 'bash --rcfile /home/pineman/.bash_run'

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# move to another monitor
bindsym $mod+Shift+Ctrl+h move workspace to output left
bindsym $mod+Shift+Ctrl+j move workspace to output down
bindsym $mod+Shift+Ctrl+k move workspace to output up
bindsym $mod+Shift+Ctrl+l move workspace to output right

# split in horizontal orientation
bindsym $mod+b split h

# split in vertical orientation
bindsym $mod+v split v

# change container layout (stacked, tabbed, toggle split)
#bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# enter fullscreen mode for the focused container
bindsym $mod+F11 fullscreen

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+z focus child

# switch to workspace
bindsym $mod+1 workspace 1
bindsym $mod+2 workspace 2
bindsym $mod+3 workspace 3
bindsym $mod+4 workspace 4
bindsym $mod+5 workspace 5
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace 1
bindsym $mod+Shift+2 move container to workspace 2
bindsym $mod+Shift+3 move container to workspace 3
bindsym $mod+Shift+4 move container to workspace 4
bindsym $mod+Shift+5 move container to workspace 5
bindsym $mod+Shift+6 move container to workspace 6
bindsym $mod+Shift+7 move container to workspace 7
bindsym $mod+Shift+8 move container to workspace 8
bindsym $mod+Shift+9 move container to workspace 9
bindsym $mod+Shift+0 move container to workspace 10

bindsym $mod+Shift+apostrophe move scratchpad
bindsym $mod+apostrophe scratchpad show

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+q exec i3-msg exit

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode
        bindsym l resize grow width 10 px or 2 ppt
        bindsym h resize shrink width 10 px or 2 ppt
        bindsym j resize grow height 10 px or 2 ppt
        bindsym k resize shrink height 10 px or 2 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
	status_command i3status
	position bottom
	font pango:Droid Sans, Icons 10
	separator_symbol "|"
	tray_output primary
	tray_padding 0
	height 20
    colors {
        background #0D0D0D
        statusline #ffffff
        separator #666666

        focused_workspace  #8F3124 #75281E #ffffff
        active_workspace   #333333 #5f676a #ffffff
        inactive_workspace #333333 #222222 #888888
        urgent_workspace   #75281E #d64937 #ffffff
    }

}

# Colors
# class                 border  backgr. text    indicator
# Defaults (blue theme)
#client.focused          #4c7899 #285577 #ffffff #2e9ef4
#client.focused_inactive #333333 #5f676a #ffffff #484e50
#client.unfocused        #333333 #222222 #888888 #292d2e
#client.urgent           #2f343a #900000 #ffffff #900000
#client.placeholder      #000000 #0c0c0c #ffffff #000000

# Red Theme
client.focused          #5E2018 #75281E #ffffff #A13729
client.focused_inactive #333333 #474d4f #ffffff #484e50
client.unfocused        #333333 #222222 #888888 #292d2e
client.urgent           #2f343a #900000 #ffffff #900000
client.placeholder      #000000 #0c0c0c #ffffff #000000

# Gaps
#gaps inner 10
#gaps outer 0
#smart_gaps on

# Autostart
exec /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
exec nm-applet
exec redshift-gtk
exec pasystray
exec urxvtd -f -q -o
exec trackpoint
exec thunar --daemon

# Keybindings
# These don't work on toshiba for no reason, use xfce4-power-manager
#bindsym XF86MonBrightnessUp exec xbacklight -set 100%
#bindsym XF86MonBrightnessDown exec xbacklight -dec 1
bindsym XF86AudioMute exec pamixer --toggle-mute
bindsym XF86AudioMicMute exec amixer set Capture toggle
bindsym XF86AudioLowerVolume exec pamixer --decrease 5
bindsym XF86AudioRaiseVolume exec pamixer --increase 5

bindsym XF86AudioPlay exec mpc toggle
bindsym XF86AudioStop exec mpc stop
bindsym XF86AudioNext exec mpc next
bindsym XF86AudioPrev exec mpc prev

bindsym XF86Display exec arandr
bindsym XF86Launch1 exec setxkbmap pt && xmodmap ~/.Xmodmap

bindsym XF86TouchpadToggle exec touchpad-toggle
#bindsym XF86Sleep exec systemctl suspend && physlock -dsm
#bindsym XF86Suspend exec systemctl hibernate && physlock -dsm

bindsym Print exec scrot -u
bindsym Ctrl+Print exec xfce4-screenshooter
bindsym $mod+f exec thunar
bindsym $mod+g exec qutebrowser --backend=webengine
bindsym $mod+p exec thunderbird
bindsym $mod+i exec owncloud
bindsym $mod+Ctrl+i exec dropbox
bindsym $mod+u exec virtualbox
bindsym $mod+y exec qalculate-gtk
bindsym $mod+t exec telegram-desktop
bindsym $mod+o exec firefox
bindsym $mod+c exec chromium
bindsym $mod+m exec java -jar /opt/shiginima/shiginima.jar
bindsym $mod+Ctrl+m exec minecraft
bindsym $mod+Tab workspace next_on_output
bindsym $mod+Shift+Tab workspace prev_on_output
bindsym $mod+F12 exec vboxmanage startvm wangblows
bindsym $mod+Ctrl+s exec skypeforlinux
bindsym $mod+s exec steam
bindsym $mod+Ctrl+d exec discord
bindsym $mod+Ctrl+n exec playonlinux
bindsym $mod+n exec wine "$HOME/.wine/drive_c/Program Files (x86)/Battle.net/Battle.net.exe"
bindsym $mod+Ctrl+r exec runescape-launcher

# Borders and things
for_window [class="^.*"] border pixel 2
for_window [class="^.*qutebrowser"] border none
for_window [class="Firefox"] border none
for_window [class="chromium"] border none
for_window [class="Runescape"] border none

# Window Rules
for_window [class="Qalculate"] floating enable
for_window [window_role="pop-up"] floating enable
for_window [title="Hearthstone"] floating enable
for_window [title="Battle.net"] floating enable
for_window [class="Wine"] floating enable

# bashrun launcher
for_window [title="bashrun"] floating enable
for_window [title="bashrun"] border pixel 3

# Assignment
assign [class="Firefox"] 10
assign [class="chromium"] 10
assign [class="^.*qutebrowser"] 10
assign [class="Thunderbird"] 9
assign [class="telegram-desktop"] 8
assign [class="skype"] 8
assign [class="Steam"] 7
assign [title="Hearthstone"] 7
assign [title="Battle.net"] 7
assign [class="Runescape"] 7

focus_on_window_activation urgent
